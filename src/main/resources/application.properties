spring.application.name=SpringSecurityJWT

# --- Configuracion de PostgreSQL ---
spring.datasource.url=jdbc:postgresql://localhost:5432/securityBd
spring.datasource.username=postgres
spring.datasource.password=juan12062000
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Configuracion de JPA / Hibernate ---create-update
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Opcional: para evitar warning de open-in-view
spring.jpa.open-in-view=false

# Si quiero cambiar el puerto
# server.port=8081